import 'package:flutter/material.dart';

class ButtonFabMoreScreen extends StatelessWidget {
  const ButtonFabMoreScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Button FAB More'),
      ),
    );
  }
}
