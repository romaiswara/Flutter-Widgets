import 'package:flutter/material.dart';

class ButtonFabMiddleScreen extends StatelessWidget {
  const ButtonFabMiddleScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Button FAB Middle'),
      ),
    );
  }
}
