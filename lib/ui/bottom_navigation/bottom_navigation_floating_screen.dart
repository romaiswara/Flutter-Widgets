import 'package:flutter/material.dart';

class BottomNavigationFloatingScreen extends StatelessWidget {
  const BottomNavigationFloatingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Bottom Navigation Floating'),
      ),
    );
  }
}
